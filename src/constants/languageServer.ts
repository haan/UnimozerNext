// Debounce window for sending textDocument/didChange to JDTLS.
export const LS_CHANGE_DEBOUNCE_MS = 150;

// LSP document versions start at 1 for newly opened files.
export const LS_INITIAL_DOCUMENT_VERSION = 1;

// LSP severity code for errors in publishDiagnostics.
export const LS_DIAGNOSTIC_SEVERITY_ERROR = 1;
