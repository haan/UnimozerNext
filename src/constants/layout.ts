// Main split: minimum width for both diagram (left) and editor (right) panels.
export const DIAGRAM_EDITOR_SPLIT_MIN_PANEL_WIDTH_PX = 260;

// Minimum height for the console panel.
export const CONSOLE_MIN_HEIGHT_PX = 120;

// Minimum height for the editor panel.
export const EDITOR_MIN_HEIGHT_PX = 240;

// Default minimum height for the top panel in stacked (top/bottom) layouts.
export const STACKED_SPLIT_TOP_MIN_HEIGHT_PX = 200;

// Minimum height for the object-bench panel.
export const OBJECT_BENCH_MIN_HEIGHT_PX = 120;

// Default minimum height for the bottom panel in stacked (top/bottom) layouts.
export const STACKED_SPLIT_BOTTOM_MIN_HEIGHT_PX = OBJECT_BENCH_MIN_HEIGHT_PX;

// Left stacked pane: minimum height for the upper UML diagram area.
export const UML_DIAGRAM_MIN_HEIGHT_PX = 240;

// Ratio distance around a snap target where we clamp to the target.
export const SPLIT_SNAP_DISTANCE = 0.03;

// Main left/right split should gravitate to an even 50/50 layout.
export const MAIN_SPLIT_SNAP_RATIO = 0.5;

// Horizontal splits should gravitate to the 75/25 layout.
export const HORIZONTAL_SPLIT_SNAP_RATIO = 0.75;
