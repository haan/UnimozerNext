// Time-to-live in seconds for pending UML -> source reveal requests.
export const UML_REVEAL_REQUEST_TTL_SECONDS = 2;

// Debounce delay before re-parsing UML from draft edits.
export const UML_PARSE_DRAFT_DEBOUNCE_MS = 300;

// Debounce delay for switching structogram method selection while moving the caret.
export const STRUCTOGRAM_METHOD_SWITCH_DEBOUNCE_MS = 120;

// Maximum status/error text length before trimming in the footer and console.
export const STATUS_TEXT_MAX_CHARS = 200;
