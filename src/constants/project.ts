// File extension used for packed Unimozer project archives.
export const PACKED_PROJECT_EXTENSION = "umz";

// Default filename used when creating a brand-new project.
export const DEFAULT_NEW_PROJECT_FILE_NAME = "NewProject.umz";

// Poll interval for detecting external file changes on disk.
export const DISK_RELOAD_POLL_INTERVAL_MS = 1500;
